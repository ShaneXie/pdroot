<?xml version="1.0" encoding="utf-8" ?>
<configuration>
  <connectionStrings>
    <clear/>
    <!-- Change the connection string below to point to the server that contains the root database - you need to create an empty root database before using this sample application -->
    <add name="rootdb" connectionString="Server=localhost;Database=theRoot;Trusted_Connection=True;User Id=sa;Password=Thisway1;" />
  </connectionStrings>
</configuration>